From: postmaster@${DOMAIN}
To: ${USER_EMAIL}
Subject: 欢迎加入！公司企业电子邮件与全局通讯录 (GAL) 设置指引
MIME-Version: 1.0
Content-Type: text/html; charset=UTF-8

<!DOCTYPE html>
<html>
<head>
<meta charset="utf-8">
<style>
  body { font-family: -apple-system, BlinkMacSystemFont, "Segoe UI", Roboto, "Helvetica Neue", Arial, sans-serif; line-height: 1.6; color: #333; }
  .card { border: 1px solid #e2e8f0; border-radius: 8px; padding: 24px; max-width: 680px; margin: 20px auto; box-shadow: 0 4px 6px -1px rgba(0, 0, 0, 0.1); }
  .header { border-bottom: 2px solid #3182ce; padding-bottom: 12px; margin-bottom: 20px; }
  .header h2 { color: #2b6cb0; margin: 0; }
  .step-box { background: #f7fafc; border-left: 4px solid #3182ce; padding: 12px 16px; margin: 16px 0; border-radius: 0 4px 4px 0; }
  code { background: #edf2f7; padding: 2px 6px; border-radius: 4px; font-family: Consolas, monospace; color: #c53030; }
  pre { background: #1a202c; color: #e2e8f0; padding: 12px; border-radius: 6px; overflow-x: auto; font-size: 0.85em; }
  .footer { margin-top: 24px; font-size: 0.85em; color: #718096; border-top: 1px solid #e2e8f0; padding-top: 12px; }
</style>
</head>
<body>
<div class="card">
  <div class="header">
    <h2>🎉 欢迎新同事加入！企业邮件与通讯录设置指引</h2>
  </div>
  <p>亲爱的 <strong>${USER_NAME}</strong> 同事您好：</p>
  <p>您的企业电子邮箱 <code>${USER_EMAIL}</code> 已正式开通！请依据下列指引配置您的 Outlook 或移动设备，以启用内部全局通讯录 (Global Address List, GAL) 联系人智能搜索功能。</p>

  <div class="step-box">
    <h3 style="margin-top:0; color:#2d3748;">📌 服务器连接信息</h3>
    <ul>
      <li><strong>收信服务器 (IMAP)：</strong> <code>${MAIL_SERVER}</code> (端口 <code>993</code> / SSL 加密)</li>
      <li><strong>发信服务器 (SMTP)：</strong> <code>${MAIL_SERVER}</code> (端口 <code>465</code> 或 <code>587</code> / SSL 加密)</li>
      <li><strong>通讯录目录 (LDAP/LDAPS)：</strong> <code>${MAIL_SERVER}</code> (端口 <code>3268</code> 免证书 或 <code>3269</code> SSL 加密)</li>
      <li><strong>登录账号：</strong> <code>${USER_NAME}</code> (仅需输入账号名称，无需输入 @${DOMAIN})</li>
      <li><strong>登录密码：</strong> 您的 Windows AD 域密码</li>
    </ul>
  </div>

  <div class="step-box">
    <h3 style="margin-top:0; color:#2d3748;">📖 Outlook 快速添加通讯录步骤</h3>
    <ol>
      <li>打开 Outlook ➔ 点击 <strong>「文件」</strong> ➔ <strong>「账户设置」</strong> ➔ <strong>「通讯簿」</strong> 标签页。</li>
      <li>点击 <strong>「新建」</strong> ➔ 选择 <strong>「Internet 目录服务 (LDAP)」</strong>。</li>
      <li>服务器名称输入 <code>${MAIL_SERVER}</code>。</li>
      <li>勾选 <strong>「此服务器要求我登录」</strong>，用户名填写 <code>${USER_NAME}</code> 与您的域密码。</li>
      <li>点击右侧 <strong>「其他设置」</strong>：
        <ul>
          <li>在「连接」标签页中，将端口改为 <strong><code>3268</code></strong>（<strong>不勾选</strong> SSL），若使用 <strong><code>3269</code></strong> 请勾选「使用安全套接字层 (SSL)」。</li>
          <li>在「搜索」标签页中，搜索基准选择 <strong>「自定义」</strong> 并输入 <code>${SEARCH_BASE}</code>。</li>
        </ul>
      </li>
      <li>点击确定并重启 Outlook 即完成！写信时输入同事姓名即可自动补齐联系人信息。</li>
    </ol>
  </div>

  <div class="footer">
    <p>本邮件由系统开户自动发送，如有任何配置疑问，请联系信息管理部 (IT Service Desk)。</p>
  </div>
</div>
</body>
</html>
